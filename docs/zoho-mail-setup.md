# Zoho Mail Setup (legacy)

> **Deprecated.** Use [Cloudflare Email Routing + Resend](./email-setup.md) instead ($0/month).

This guide is kept for reference only if you choose paid Zoho Mail Lite (~€0.90/month).

See [Zoho Mail pricing](https://www.zoho.com/mail/zohomail-pricing.html).

Legacy env vars (no longer used by the app):

```bash
SMTP_HOST=smtp.zoho.eu
SMTP_PORT=587
SMTP_USER=info@himalayan-sound.com
SMTP_PASS=your-zoho-app-password
```
